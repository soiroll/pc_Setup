<% tp.file.cursor() %>
<%*
/*
目的：
- 当日に作成・更新したノートを取得する
  - 作成日が本日のノート → 通常リスト
  - 更新日が本日のノート → 「## Updated」配下にリスト
  を分けて表示する
*/

const createdHeader = `
<div style="border-top: solid medium green; border-bottom: solid medium green; text-align: center;">
    <font size="6"><strong>
        📚Created Notes
    </strong></font>
</div>
`;

const updatedHeader = `
<div style="border-top: solid medium green; border-bottom: solid medium green; text-align: center;">
    <font size="6"><strong>
        ✒Updated Notes
    </strong></font>
</div>
`;

// ---------- 当日の範囲を決定 ----------

const today = moment().startOf('day');
const tomorrowStart = moment().endOf('day').add(1, 'ms');

// ---------- 判定関数 ----------

// 作成日判定（当日）
const isCreatedToday = (file, today, tomorrowStart) =>
  file.stat && moment(file.stat.ctime).isBetween(today, tomorrowStart, null, '[)');

// 更新日判定（当日）
const isUpdatedToday = (file, today, tomorrowStart) =>
  file.stat && moment(file.stat.mtime).isBetween(today, tomorrowStart, null, '[)');

// 公開ノート判定
const isPublicNote = (file) =>
  !file.path.startsWith("_") && file.extension === "md";

// ---------- ファイル取得 ----------

let files = this.app.vault.getFiles()
  .sort((a, b) => a.basename.localeCompare(b.basename));

// ---------- 作成ノート（当日） ----------

const createdFiles = files.filter(file =>
  isPublicNote(file) &&
  isCreatedToday(file, today, tomorrowStart)
);

if (createdFiles.length > 0) {
  tR += "\n" + createdHeader + "\n";
  tR += createdFiles
    .map(file => `- [[${file.basename}]]`)
    .join("\n");
}

// ---------- 更新ノート（当日） ----------

const updatedFiles = files.filter(file =>
  isPublicNote(file) &&
  !isCreatedToday(file, today, tomorrowStart) &&
  isUpdatedToday(file, today, tomorrowStart)
);

if (updatedFiles.length > 0) {
  tR += "\n\n" + updatedHeader + "\n";
  tR += updatedFiles
    .map(file => `- [[${file.basename}]]`)
    .join("\n");
}
%>
