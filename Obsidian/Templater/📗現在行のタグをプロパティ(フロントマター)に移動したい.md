<%*
const KEY = 'tags'

const { editor, metadataEditor } = app.workspace.activeEditor
const currentLine = editor.getLine(editor.getCursor().line)
const tags = currentLine.split(" ")
  .filter(x => x.startsWith("#"))
  .map(x => x.slice(1))

if (tags.length === 0) {
    return
}

metadataEditor.insertProperties({tags})
%>